{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default Sourcerer capability set (Phase 0 scaffold).",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
